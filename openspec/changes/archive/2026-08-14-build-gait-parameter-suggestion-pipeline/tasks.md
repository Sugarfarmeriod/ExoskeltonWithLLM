## 1. Speedgoat 数据采集

- [x] 1.1 确认 Speedgoat/Simulink Real-Time 目标机连接方式、目标机名称或 IP，以及当前模型部署流程。
- [x] 1.2 记录现有人工流程：Ctrl+B 编译 `.mldatx`，SLRT Explorer 上传、运行和修改参数。
- [x] 1.3 在 Simulink 模型中确定并命名需要记录的信号：`q_meas`、`dq_meas`、`q_ref`、`dq_ref`、`FeetLoad`、`Phi`、`Phase0Event`、`tau_e`、`SafeTorque`、助力使能状态和参数快照。
- [x] 1.4 配置 Simulink Real-Time File Log 或等价正规记录机制，确保试次结束后日志保存在目标机文件系统。
- [x] 1.5 编写或整理 MATLAB 脚本，用于导入目标机日志，并归档到本地 `data/raw/<trial_id>/`；外骨骼工程模型的上传、启动、停止和在线调参仍通过 SLRT Explorer 人工完成。
- [x] 1.6 将下载日志导出为后续 Python 可读取的 CSV 或 MAT，并保留时间戳、采样率、模型名、患侧和参数快照。

## 2. 数据契约与样例

- [x] 2.1 定义规范试次输入 schema，以及 CSV/MAT 导出中必须包含的字段。
- [x] 2.2 添加一个小型样例或合成试次 fixture，包含实测运动、参考运动、足底负载/接触、相位/事件、力矩和元数据字段。
- [x] 2.3 实现可从项目根目录运行的试次加载与校验逻辑，并在缺字段时输出清晰错误。
- [x] 2.4 文档化预期导出格式和输出路径。

## 3. 试次步态分析

- [x] 3.1 实现基于足底负载/接触信号的确定性步态事件检测。
- [x] 3.2 实现 `Phase0Event` 辅助的周期边界检测和交叉校验。
- [x] 3.3 实现步态周期切分、质量检查和有效周期计数。
- [x] 3.4 实现健康侧、健康人模板或历史较优试次的参考模板选择。
- [x] 3.5 实现患侧-参考对齐，并计算相位延迟、峰值屈曲误差、tracking RMSE、力矩峰值、力矩 RMS 和力矩限幅比例。
- [x] 3.6 写出紧凑 JSON 分析结果，包含可靠性标志和不可用字段原因。

## 4. 受约束参数建议

- [x] 4.1 定义可配置阈值：相位延迟、峰值屈曲误差、tracking RMSE、有效周期数和力矩安全否决阈值。
- [x] 4.2 为 `phase_offset`、`Amplitude`、助力增益、`MO_Kp` 和 `MO_Kd` 实现有边界的建议动作。
- [x] 4.3 实现带相位可靠性否决的 `phase_offset` 建议规则。
- [x] 4.4 实现带 `SafeTorque`、Torque_Guard 和饱和否决的幅值与助力建议规则。
- [x] 4.5 实现带 tracking error 和力矩余量约束的阻抗增益建议规则。
- [x] 4.6 写出 `parameter_suggestion.json`，包含规则 ID、来源指标、阈值、否决原因和保守保持结果。

## 5. LLM 诊断报告

- [x] 5.1 实现只从指标 JSON 和建议 JSON 生成报告。
- [x] 5.2 Markdown 报告必须包含试次身份、数据质量、关键指标、建议摘要、安全检查和人工确认状态。
- [x] 5.3 添加报告校验，标记无法追溯到指标字段或规则输出的结论。
- [x] 5.4 当调用方只提供原始长序列数据、没有结构化指标时，拒绝生成报告。

## 6. 验证与文档

- [x] 6.1 为加载、缺字段处理、周期切分质量、安全否决和报告可追溯性添加聚焦测试或脚本检查。
- [x] 6.2 在样例 fixture 上运行完整流程，确认 JSON 和报告产物能够生成。
- [x] 6.3 更新 README，说明如何运行步态分析和参数建议流程。
- [x] 6.4 记录仍需用户或导师确认的阈值和参考源决策。
