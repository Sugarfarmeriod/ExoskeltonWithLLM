# 论文主线入口

> **2026-08-14 当前决策（优先于下文旧路线）**  
> 新论文默认研究“实时环外、受约束、按试次/窗口运行的 LLM Agent”。Agent 调用确定性的试次分析、评价、安全检查和参数建议工具，输出需经人工确认的下一试次方案；不直接生成实时力矩，也不自动向 Speedgoat 写参数。实验不再负责证明“LLM 是否必要”。下文关于“唯一主线是低频优化层”以及用对照回答“为什么需要 LLM”的内容视为历史方案，待论文合同重写时删除。论文线的当前规则见 `paper/AGENTS.md`，实验线见 `experiment/AGENTS.md`。

更新日期：2026-07-16

用途：每次隔一段时间重新回到论文工作时，先读本文件。它是当前论文主线、事实边界和进度状态的唯一入口；其他文档是它的支撑材料。

## 1. 三十秒结论

这篇新论文不应再写成“提出 DMP-阻抗外骨骼控制”，因为既有中文论文已经完成了准直驱膝关节外骨骼、在线增量 DMP 和柔顺控制的工程验证。

当前最可行的新论文主线是：

> 在保留 `TestStructure.slx` 确定性实时力矩环和 `Torque_Guard` 最终安全权限的前提下，构建一个位于实时环外、按试次或步态窗口低频运行的有界参数优化层，使其根据可测评价信号调整 DMP 时机/幅值和助力强度，并与固定参数或人工调参基线比较。

“AI 应放在哪里”只是研究动机；真正能够构成论文贡献的是**优化方法、参数映射、系统实现和实验结果**。

## 2. 现在能不能直接写 Introduction

可以写一版中文工作稿，但还不能把它视为投稿定稿。

| 内容 | 当前状态 | 能否落笔 |
|---|---|---|
| P1-P3：外骨骼需求、同步/安全难点、现有控制底座 | 事实与文献框架已具备 | 可以 |
| P4-P5：人工调参瓶颈与低频优化文献 | 语料和候选文献已具备，具体引用仍需逐条核验 | 可以写带引用占位符的工作稿 |
| P6：精确研究缺口 | 依赖最终优化器、参数集合和验证层级 | 只能暂定 |
| P7：本文贡献 | 方法和实验尚未冻结 | 只能写条件式版本 |
| “提高/降低了某指标”等结果性陈述 | 尚无新论文实验结果 | 不能写 |

因此，正确顺序不是继续扩大检索，也不是马上英文定稿，而是先完成一次“论文合同”确认，然后精读最多 5 篇核心文献并写中文 Introduction v0。实验结果出来后再重写 P6-P7，最后进行英文独立改写。

## 3. 必须保持一致的事实

### 3.1 既有中文论文已经完成的工作

- 准直驱膝关节外骨骼硬件与 MATLAB/Simulink + Speedgoat 实时控制基础。
- 基于参考侧关节运动的 AFO、分时在线增量 DMP 和阻抗/柔顺控制。
- 健康受试者条件下的平地、坡道及变频/变幅等工程实验。
- 这部分用于证明硬件与控制底座可行，不用于宣称卒中临床疗效。

### 3.2 当前正式实验系统

- 最新正式模型：`TestStructure.slx`。
- 当前 AO **仅依赖足底压力**，输出 `FeetLoad`、`Phase0Event`、`Phi` 和 `Omega`。
- 当前 AO 不是中文论文 AFO 的后续版本，两者不能混写。
- 当前实时链路：

```text
足底压力
  -> AO（接触/周期/相位）
  -> DMP（q_ref、dq_ref）
  -> TorqueController / PD 或阻抗控制
  -> 总助力增益与力矩使能
  -> Torque_Guard / SafeTorque / Saturation
  -> 膝关节力矩输出
```

### 3.3 当前已有但尚未形成新论文结果的材料

- 108 条论文矩阵记录；92 条有可靠文本缓存；63 篇提取到可分析的引言段落。
- 5 个大型真实 MATLAB Dataset 已归档，但尚未完成统一信号映射、受试者/工况说明和规范化分析。
- Speedgoat File Log、MAT 回放和离线报告流程已经搭建。
- 参数建议流程目前只在合成样例上跑通，属于确定性规则，不是已经实现的 BO、CMA-ES、RL 或其他 AI 优化器。

### 3.4 已确定的人体实验与传感器边界

- 导师当前规划招募 8 名健康受试者；这是预计样本量，实验完成前不得写成“8 人已完成”。年龄范围等纳入标准仍待正式协议确认。
- 论文定位为健康受试者条件下的控制性能、可行性和个体化参数优化验证，不是患者康复或临床疗效研究。
- 系统实现和论文均不包含 EMG 或 IMU；优化输入与评价指标必须来自足底压力、AO 输出、膝角度、膝速度、参考轨迹和膝力矩等现有信号。
- 模型中的 `sEMGMuxIn` 是历史端口名，当前实际承载足底压力/电压通道，不能在论文中解释为 sEMG 输入。
- 招募和正式采集前仍需落实伦理审批/豁免、知情同意、风险处置、匿名化和数据管理要求。

## 4. 新论文要新增什么

建议的新层级如下：

```text
已完成试次或步态窗口
  -> 计算同步、运动学、力矩与安全指标
  -> 低频受约束优化器提出下一组参数
  -> 参数边界、变化率、安全否决与人工确认
  -> 下一试次更新参数
  -> TestStructure.slx 实时控制链照常运行
```

优化器不直接输出实时力矩，也不修改 `Torque_Guard` 的最终否决权。

仅画出上述架构通常不足以构成有说服力的新论文。至少还需要：

1. 一个明确且可复现的优化算法。
2. 一个无退化解的目标函数和硬安全约束。
3. 一组有工程依据的参数边界与更新规则。
4. 与固定参数、人工调参或简单搜索基线的比较。
5. 在离线回放、半实物或真实外骨骼中的至少一种闭环验证；最好形成分层验证链。

## 5. 推荐的论文合同

以下是当前最稳的默认方案，尚需用户确认后才作为正式写作前提。

### 研究问题

> 在不改变准直驱膝关节外骨骼实时安全力矩环的条件下，低频有界优化能否在有限试次内改善可复现的辅助评价目标，并且不增加安全约束违反？

### 方法定位

- 首选：按试次更新的约束 Bayesian optimization；原因是人体试次昂贵、参数维度应较低。
- 备选：CMA-ES，用于目标噪声较大或后续开放更多参数的情况。
- 当前不建议：直接实时 RL 力矩控制或让 LLM 产生力矩。

### 第一阶段参数集合

- 推荐先开放 `phase_offset`、`Amplitude` 和一个经确认的总助力强度变量，例如 `MO_OverwriteKpGain` 或正式定义的 `assist_gain` 映射。
- 暂时固定 `tau`、`MO_Kp`、`MO_Kd`；待稳定性边界和实验负担评估后再扩展。
- `SafeTorque` 和最终饱和限幅不是优化目标变量，而是不可越过的安全约束。

### 目标函数原则

目标函数必须包含一个独立于被优化参数的任务收益项，例如相对对侧/固定模板的运动学目标、双侧对称性或经过确认的辅助任务指标，再加入同步/跟踪项和安全惩罚。所有指标必须能由足底压力、AO 输出、膝角度、膝速度、参考轨迹和膝力矩计算，不得把 EMG 或 IMU 指标写入本研究方法。

不能只最小化跟踪误差与力矩：若 `Amplitude` 或助力增益也可被优化，这种写法可能把“零幅值、零助力”变成看似最优的退化解。

### 推荐基线

- 固定默认参数。
- 可复现的人工调参方案。
- 简单网格/随机搜索或无优化方案，用于区分“优化器有效”与“只要换参数就有效”。

## 6. 全文结构与当前状态

| 章节 | 该章必须回答的问题 | 当前状态 |
|---|---|---|
| Introduction | 为什么是受约束低频调参问题，本文具体补什么缺口？ | 七段式蓝图完成，正文未写 |
| Related Work | 控制底座、HIL/BO/CMA-ES、状态估计与语义接口之间是什么关系？ | 论文矩阵完成，主题综合待写 |
| System and Problem Formulation | 当前 AO-DMP-PD/阻抗-安全链是什么，优化变量和约束如何数学化？ | 系统材料已有，正式问题定义待写 |
| Optimization Method | 优化器如何提出参数、处理噪声、约束和停止条件？ | 未实现/未冻结 |
| Experimental Protocol | 用什么对象、任务、基线、指标和安全流程验证？ | 已定计划为 8 名健康受试者且无 EMG/IMU；年龄范围、任务、工况、重复次数、随机化、统计分析与伦理流程待定 |
| Results | 是否改善主要目标、收敛多快、是否触发安全约束？ | 尚无新论文结果 |
| Discussion | 为什么采用慢优化而非实时 AI，结论能推广到哪里？ | 论证素材已有，需等待结果 |
| Conclusion | 本文实际证明了什么、没有证明什么？ | 等待结果 |

## 7. ARS 阶段看板

| ARS 阶段 | 状态 | 已有交付物/缺口 |
|---|---|---|
| Stage 1 Research | 广泛检索和轻量筛选已完成 | 矩阵、研究路线、引言模式；核心论断的逐篇深读仍待进行 |
| Stage 2 Write - Configuration | 部分确认 | 8 名健康受试者计划与无 EMG/IMU 已固定；目标函数、算法、参数集、基线和投稿对象待冻结 |
| Stage 2 Write - Architecture | Introduction 已完成 | 七段式 outline 与 evidence map 已有 |
| Stage 2 Write - Argumentation | Introduction 已完成 | CER 论证蓝图和风险检查已有 |
| Stage 2 Write - Drafting | 尚未开始 | 先写中文 v0，再随方法和结果回改 |
| Stage 2.5 Integrity 及以后 | 尚未进入 | 正文完成后进行引用/数据核验和模拟审稿 |

## 8. 材料阅读顺序

### 现在只读这三份

1. `PAPER_START_HERE.md`：掌握全局和当前决策。
2. `outputs/ars_introduction_plan_hybrid_zh.md`：看 Introduction 七段结构、论证链和风险。
3. `outputs/introduction_pattern_report.md`：理解 63 篇可分析引言中提炼出的共同写法；它不能直接充当引用证据。

### 做方法与实验时再读

- `analysis_outputs/InterForceControlTask_parameter_report.md`
- `docs/open_decisions.md`
- `docs/trial_data_schema.md`

### 选引用时再读

- `outputs/paper_matrix.csv`
- `outputs/top_10_must_read.md`
- `outputs/manual_deep_read_list.md`

不要为了开始写作而重新通读全部论文。正式起草每次只精读与当前论断最相关的 3-5 篇，并把每条引用限制在原文真正支持的范围内。

## 9. 接下来的固定顺序

1. 确认或修改第 5 节的论文合同。
2. 为 Introduction 的 P1-P6 建立“论断 -> 文献”引用包，第一轮精读不超过 5 篇。
3. 写中文 Introduction v0，P6-P7 对未冻结内容明确标注占位符。
4. 冻结优化器、目标函数、参数边界、更新频率和基线。
5. 完成伦理、知情同意和安全流程，规范化真实数据并完成离线/回放验证，再进入计划的 8 名健康受试者正式外骨骼实验。
6. 用结果重写 Introduction 的 gap/贡献，完成 Method、Results 和 Discussion。
7. 对中文逻辑稿进行英文独立改写，而不是逐句直译。

当前最近的写作门槛只有一个：确认第 5 节是否作为论文合同。确认后即可开始核心文献精读和中文 Introduction v0。
