# AGENTS.md

## 项目目标与当前论文前提

本项目研究“受约束 LLM Agent 如何接入膝关节外骨骼 DMP-阻抗控制框架”。论文默认 **LLM Agent 是研究对象且有必要引入**，不再把“证明是否需要 LLM”作为实验门槛。

LLM Agent 位于实时力矩环之外，按试次或步态窗口低频工作：理解实验目标与穿戴者反馈，调用确定性分析/评估/参数建议工具，生成安全、可解释、可追溯的下一试次方案。它不得直接生成实时力矩，不得绕过安全约束，也不得自动向 Speedgoat 写入参数。

实验应回答 Agent 是否正确、安全、可追溯，以及是否改善目标指标；不要求用实验为“LLM 是否必要”辩护。

## 不可混淆的事实边界

- 设备：准直驱下肢膝关节外骨骼。
- 实时平台：MATLAB/Simulink + Speedgoat；正式实验模型为 `TestStructure.slx`。
- 当前链路：足底压力 → AO（`FeetLoad`、`Phase0Event`、`Phi`、`Omega`）→ DMP → TorqueController/PD 或阻抗控制 → Torque_Guard → 力矩输出。
- 当前 AO 仅依赖足底压力，不是既有中文论文中基于参考侧关节运动的 AFO 的后续版本。
- 可调参数：`Amplitude`、`tau`、`phase_offset`、`MO_Kp`、`MO_Kd`、`MO_OverwriteKpGain`、`SafeTorque` 及 Saturation limits。
- 监控信号：`Phi`、`Phase0Event`、`FeetLoad`、`q_meas`、`dq_meas`、`q_ref`、`dq_ref`、`tau_e`。
- 本项目不使用 EMG 或 IMU；历史端口名 `sEMGMuxIn` 当前承载足底压力/电压信号。
- 导师规划招募 8 名健康受试者，这是计划样本量；年龄范围等纳入标准仍待正式协议确认。

## 工作线与指令入口

- 论文调研、论文阅读、论证与写作：必须同时遵守 `paper/AGENTS.md`。
- 实验设计、数据、系统实现与验证：必须同时遵守 `experiment/AGENTS.md`。
- 跨线决策、任务拆解、结果合并和状态维护由根任务负责。
- 任务登记与命名规则见 `coordination/threads.md`。

论文线同时包含调研和写作，但应拆成边界清楚的子任务：调研负责证据，写作负责把已核验的证据组织成论证，不允许写作任务自行补造文献事实。

## 多任务工作流

用户可见的 Codex 子任务是首选协作单元；不要把临时 subagent 当作长期项目任务。根任务作为总指挥，负责拆解、派发、等待、审查、合并和归档。

模型分配：

- 论文调研、全文理解、方法资产提取、审稿和论文写作：`gpt-5.6-sol`，`medium`。
- 实验设计、系统边界、安全审查和结果解释：`gpt-5.6-sol`，`medium`。
- 编码、测试、批处理和机械性数据处理：`gpt-5.6-luna`，`max`。

任务名统一为：`<派发者>-<工作线>-<类型>-<简短目的>`。例如：`ROOT-论文-调研-提取工具资产`、`ROOT-实验-代码-实现工具注册表`。若由子任务继续派发，首段改为该任务的短名。

创建任务后登记到 `coordination/threads.md`。任务只有在交付物已合并或明确不再采用、没有未解决问题、无需用户继续检查时才能归档；需要用户选择或仍有未交付内容的任务不得自动归档。

## OpenSpec 使用边界

OpenSpec 用于边界明确、需要验收标准的工程变更，例如数据契约、工具注册表、Agent 调用流程、安全校验和日志追溯。文献阅读、研究问题讨论、录音理解和论文段落写作不使用 OpenSpec。

新功能在范围稳定后再建立独立 change；不要把仍在讨论的科研方向提前固化成大型工程规格。已完成的 change 应在规格同步后及时归档。

## 协作判断原则

- 不把用户提出的修改意见默认视为正确结论。执行前先检查其与研究目标、事实边界、现有证据、实验可实现性和前后文逻辑是否一致。
- 如果修改存在歧义、代价、证据不足或会扩大论文承诺，应明确指出并给出理由；必要时提出反例、替代方案或向用户反问，而不是直接迎合修改。
- 区分“讨论需求”和“授权修改”：用户用于澄清想法或询问可行性时，先完成对齐和判断，不自动把讨论内容写入论文、规格或代码。
- 对用户意见提出质疑时，应给出可核查的依据和具体影响；不为反对而反对，确认合理后再执行。

## 共同执行规则

- 任何论文结论都要能追溯到 PDF、摘要或元数据，并明确证据等级；不得编造 DOI、实验数据、受试者数量或指标。
- 不一上来全文读取所有 PDF。优先复用 `text_cache`、evidence cards、矩阵和已有笔记；单轮最多精读 5 篇、处理 30 篇 evidence cards。
- 批量抽取和检索优先使用本地脚本，解释性判断由高智能任务完成。
- 保留用户已有改动，不覆盖无关文件，不把 PDF 或受试者数据上传到公共仓库。
- 任何人体实验、参数下发和硬件运行都必须有人类确认并遵守伦理、安全与数据管理要求。
